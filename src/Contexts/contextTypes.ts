export type JColors = {
    radius: string;
    background: string;
    foreground: string;
    accentBackground: string;
    border: string;
    accentForeground: string;
    chatBackground: string;
};
export type User = {
    username: string;
    email: string;
    userId: string;
    privileges: Array<string>;
};
