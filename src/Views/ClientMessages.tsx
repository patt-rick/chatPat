import Care from "../components/Care";
import PageWrapper from "../components/PageWrapper";

const ClientMessages = () => {
    return (
        <PageWrapper title={"Messages"} subTitle="Serve all your clients on time">
            <Care />
        </PageWrapper>
    );
};

export default ClientMessages;
